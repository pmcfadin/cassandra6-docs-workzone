# Antora playbook for Cassandra 6 Docs Workzone
# This builds a local preview of draft documentation.
# It is NOT the official Cassandra docs build.
#
# Usage:
#   npx antora antora-playbook.yml
#
# Output: build/site/

site:
  title: 'Apache Cassandra 6 Docs — Preview'
  url: ~
  start_page: home::index.adoc

content:
  sources:
    - url: .
      start_path: content/home
      branches: HEAD
    - url: .
      start_path: content/operators
      branches: HEAD
    - url: .
      start_path: content/developers
      branches: HEAD
    - url: .
      start_path: content/contributors
      branches: HEAD
    - url: .
      start_path: content/reference
      branches: HEAD
    # Upstream Apache Cassandra trunk docs — provides all "unchanged" pages
    # that the workzone didn't import (CQL commands, compaction strategies,
    # getting-started, architecture, vector-search, data-modeling, etc.)
    - url: https://github.com/apache/cassandra.git
      start_path: doc
      branches: trunk

ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true

output:
  dir: ./build/site

runtime:
  fetch: true

= Operator Quickstart

[IMPORTANT]
====
*Preview | Unofficial | For review only*
====

Use this page to get a single Cassandra node running quickly, then
confirm that you can connect and clean up afterward.

== Local Evaluation with Docker

. Pull the image:

[source,bash]
----
docker pull cassandra:latest
----

. Start Cassandra:

[source,bash]
----
docker run --name cassandra -d -p 9042:9042 cassandra:latest
----

. Wait about 30 seconds for startup, then connect:

[source,bash]
----
docker exec -it cassandra cqlsh
----

. Check the node state:

[source,bash]
----
docker exec -it cassandra nodetool status
----

You should see one node listed with status `UN`:

[source,console]
----
Datacenter: datacenter1
=======================
Status=Up/Down
|/ State=Normal/Leaving/Joining/Moving
--  Address     Load      Tokens  Owns (effective)  Host ID                               Rack
UN  172.17.0.2  123.45 KiB  1      100.0%            01234567-89ab-cdef-0123-456789abcdef  rack1
----

To clean up the evaluation container, stop and remove it:

[source,bash]
----
docker rm -f cassandra
----

== Production Pilot

Use a production pilot when you want to validate a single host before
you build out a cluster.

. Follow the xref:install.adoc[Installation Guide].
. Set `listen_address` and `rpc_address` in `cassandra.yaml`.
. Start Cassandra with your service manager.
. Verify the node reaches `UN` before you continue.

== Production Cluster

Use this path when you are building the real cluster.

. Plan rack and datacenter placement before you install anything.
. Follow the xref:install.adoc[Installation Guide] on every node.
. Configure `cluster_name`, seeds, and node addresses consistently.
. Start one node at a time and wait for each node to reach `UN`.

== Next Steps

* xref:deployment-guide.adoc[Choose Your Deployment Model]
* xref:install.adoc[Installing Cassandra]
* xref:configure.adoc[Configuration]
* xref:production.adoc[Production Recommendations]
* xref:developers::quickstart.adoc[Developer Quickstart]
